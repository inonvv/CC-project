Run a security audit using the @readonly-auditor agent.
Focus on recently changed files unless $ARGUMENTS says "full".

For a full audit, scan all packages. For a targeted audit,
detect relevant packages from recent git changes.
